class Solution {
public:
    string pushDominoes(string dominoes) {
        int len = dominoes.size();
        int cur = 0;
        int prev = 0;
        while(cur < dominoes.size()){
            if(dominoes[cur] == '.') continue;
            if(dominoes[cur] == 'L'){
                if(prev < cur){
                    
                }
            }
        }
    }
};